#Leia um valor inteiro em segundos, e imprima-o em horas, minutos e segundos.

segundos = int(input("Digite um valor inteiro em segundos"))

horas = segundos // 3600
resto = segundos % 3600
minutos = resto // 60
seg = resto % 60

print(f"{horas}H {minutos}MIN {seg}S")
